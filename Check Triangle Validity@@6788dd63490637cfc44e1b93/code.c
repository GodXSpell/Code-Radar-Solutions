#include <stdio.h>
#include <math.h>

int main() {
    int a,b,c res;
    scanf("%d %d %d",&a,&b,&c);
    a=math.pow(a,2);
    b=math.pow(b,2);
    c= math.pow(c,2);
    if (c = (a+b)){
        printf("Valid");

    }else{
        printf("Not Valid");
    }
    return 0;
}